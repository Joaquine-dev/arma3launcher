{
  "name": "ar3url-launcher-server",
  "version": "1.0.0",
  "description": "Serveur de mods pour AR3URL Launcher",
  "type": "module",
  "scripts": {
    "generate-manifest": "node setup-server-manifest.js .",
    "install-server": "npm install && echo 'Installation terminée. Copiez les fichiers vers /usr/share/caddy/mods/'"
  },
  "dependencies": {
    "fs-extra": "^11.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
